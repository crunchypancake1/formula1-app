{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "formula1-bot",
  "main": "./src/index.ts",
  "compatibility_date": "2026-08-08",
  "compatibility_flags": ["nodejs_compat"],

  "workers_dev": false,
  "preview_urls": false,

  // 1-minute is Cron Triggers' minimum granularity — the reason session
  // cards are finalize-only rather than continuously re-edited.
  "triggers": {
    "crons": ["* * * * *"]
  },

  "vars": {
    "DISCORD_GUILD_ID": "1301861026844643409",
    "DISCORD_CHANNEL_NAME": "active-session"
  },

  // Discord bookkeeping that would be overkill in Postgres: the team-role map
  // is twelve ids nothing joins against, so it lives here instead of behind
  // Hyperdrive and the tunnel. See `src/discord/roleStore.ts`.
  "kv_namespaces": [
    {
      "binding": "BOT_STATE",
      "id": "f95fefa1c9e245daaa8ed60a9230579b"
    }
  ],

  "hyperdrive": [
    {
      "binding": "HYPERDRIVE",
      "id": "ded8933c250e438cac3a2d76b2f97b5e",
      "localConnectionString": "postgresql://postgres:changeme@localhost:7005/f1_app"
    }
  ],

  "secrets_store_secrets": [
    {
      "binding": "DISCORD_BOT_TOKEN",
      "store_id": "d947ac5bb8ef4800ac46fc59128a1a09",
      "secret_name": "discord-bot-token"
    }
  ],

  "routes": [{ "pattern": "f1-bot.crunchypancake.com", "custom_domain": true }],

  "observability": {
    "logs": { "enabled": true, "invocation_logs": true }
  }
}
